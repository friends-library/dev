[dev]
  command = "pnpm run dev"

[build]
  command = "pnpm run build"
  publish = "dist/"
  functions = "src/functions"
  ignore = "npx nx-ignore --verbose @friends-library/jones"

[build.environment]
  NODE_VERSION = "18.13.0"
